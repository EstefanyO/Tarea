#include <iostream>
#include "Linea.h"

double Linea::mueveDerecha(double _dato) {
     // en este método. se debe utilizar _dato como el numero de unidades que se quiere desplazar la recta en dirección las coordenadas en x,
     // es decir _dato+A1 y _dato+B1 donde las nuevas coordenadas serán (_dato + A1, A2) y (_dato + B1, B2).
}
double Linea::mueveIzquierda(double _dato) {
     // hacer lo mismo que en el m[etodo anterior pero el movimiento va hacia la izquierda
}
double Linea::mueveArriba(double _dato) {
     // hacer lo mismo que en el m[etodo anterior pero el movimiento va hacia arriba
}
double Linea::mueveAbajo(double _dato) {
     // hacer lo mismo que en el m[etodo anterior pero el movimiento va hacia abajo
}
double Linea::informacion() {
     double recta[2];    // array o lista que va a almacenar la lista del punto A y la del punto B.
     double puntoA[2];   // array o lista que va a almacenar las coordenadas del punto A.
     double puntoB[2];   // array o lista que va a almacenar las coordenadas del punto A.
     
     Punto* A=A[0]       // aquí se debe llamar el metodo get para obtener las coordenadas de los dos puntos de las recta 
                         // meter las coordenadas del punto A en puntoA[] y las del punto B en puntoB[], a su vez meter ambos arrays o listas
                         // en recta[]
     
}
